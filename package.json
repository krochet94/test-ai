{
  "name": "test_ai",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server",
    "vercel-build": "npm run postbuild && cd client && npm run vercel-build",
    "install-client": "cd client && npm install --legacy-peer-deps",
    "postbuild": "npm install --legacy-peer-deps && npm run install-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "engines": {
    "node": "16.20.2",
    "npm": "8.19.4"
  }
}
