{
  "name": "test_ai",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server",
    "vercel-build": "npm run vercel-postbuild && cd client && npm run build",
    "install-client": "cd client && npm install --legacy-peer-deps",
    "vercel-postbuild": "npm install && npm run install-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
